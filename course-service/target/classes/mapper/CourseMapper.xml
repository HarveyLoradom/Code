<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.course.mapper.CourseMapper">
    <resultMap id="CourseResultMap" type="com.campus.course.entity.Course">
        <id property="id" column="id"/>
        <result property="courseCode" column="course_code"/>
        <result property="courseName" column="course_name"/>
        <result property="teacherId" column="teacher_id"/>
        <result property="teacherName" column="teacher_name"/>
        <result property="credit" column="credit"/>
        <result property="capacity" column="capacity"/>
        <result property="selectedCount" column="selected_count"/>
        <result property="schedule" column="schedule"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="selectById" resultMap="CourseResultMap">
        SELECT c.*, u.real_name as teacher_name 
        FROM course c 
        LEFT JOIN user u ON c.teacher_id = u.id 
        WHERE c.id = #{id}
    </select>

    <select id="selectAll" resultMap="CourseResultMap">
        SELECT c.*, u.real_name as teacher_name 
        FROM course c 
        LEFT JOIN user u ON c.teacher_id = u.id
    </select>

    <insert id="insertCourse" parameterType="com.campus.course.entity.Course">
        INSERT INTO course (course_code, course_name, teacher_id, credit, capacity, selected_count, schedule, created_at)
        VALUES (#{courseCode}, #{courseName}, #{teacherId}, #{credit}, #{capacity}, #{selectedCount}, #{schedule}, NOW())
    </insert>

    <update id="updateCourse" parameterType="com.campus.course.entity.Course">
        UPDATE course
        SET course_code = #{courseCode}, course_name = #{courseName}, teacher_id = #{teacherId},
            credit = #{credit}, capacity = #{capacity}, selected_count = #{selectedCount}, schedule = #{schedule}
        WHERE id = #{id}
    </update>

    <delete id="deleteCourse">
        DELETE FROM course WHERE id = #{id}
    </delete>
</mapper> 